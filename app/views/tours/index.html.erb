<h1>Tours#index</h1>
<p>Find me in app/views/tours/index.html.erb</p>

<ul>
    <% @tours.each do |a| %>

    <li>
        <%= link_to(a.name, "/tours/#{a.id}") %>
    </li>
    <li>
        <p></p>
        <%= image_tag(a.image) %>
        <p>
            <%= "Cities for the tour: #{a.theme}" %>
        </p>
        <p>
            <%= "Main highlights: #{a.highlights}" %>
        </p>

    </li>

    <% end %>
</ul>
